const hardhat = require('hardhat');

const { signer } = require('../../utils/init');

const INSTANCE_ADDRESS = '0x379c96e2D392608cB6FF58a7109811B5ea6d4694';

const main = async () => {
    // Deploy a destruct with funds.
    const SelfDestruct = 0;
};
